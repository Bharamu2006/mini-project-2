name: hello world demo

on:
 push:
  branches：
    -   main

  jobs:
   say-hello:
     runs-on: ubuntu-lacest
     steps:
      -   name: checkout code
          uses: actions/checkout@v4

      -    name: Print Hello
           run: echo "Hello , Github Actions"
   
